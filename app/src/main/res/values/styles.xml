<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/orange</item>
    </style>

    <style name="ListView" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:paddingLeft">@dimen/stickylist_vertical_padding</item>
        <item name="android:paddingRight">@dimen/stickylist_horizontal_padding</item>
        <item name="android:layout_marginLeft">@dimen/stickylist_vertical_padding</item>
    </style>
</resources>
